---
name: Image

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]
  release:
    types: ["published"]
  workflow_dispatch: {}

permissions: {}

jobs:
  image:
    name: Build${{ github.event_name == 'release' && ' and push' || '' }}
    runs-on: ubuntu-26.04
    permissions:
      contents: read
      packages: write
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - name: Install Nix
        uses: DeterminateSystems/nix-installer-action@3138316df39ed29be04236d7ffc686fa525866aa # v23

      - name: Build image
        run: nix build --accept-flake-config --print-build-logs .#image

      - name: Push image
        if: github.event_name == 'release'
        env:
          IMAGE: ghcr.io/${{ github.repository }}
          REGISTRY_USER: ${{ github.actor }}
          REGISTRY_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          RELEASE_TAG: ${{ github.event.release.tag_name }}
        run: |
          set -euo pipefail
          version="${RELEASE_TAG#v}"
          ./result | nix run nixpkgs#skopeo -- copy --insecure-policy \
            --dest-creds "${REGISTRY_USER}:${REGISTRY_TOKEN}" \
            docker-archive:/dev/stdin "docker://${IMAGE}:${version}"
